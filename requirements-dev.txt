# 분석 파이프라인 재현용 — 전처리·가설검증·벤치마크·최적화 스크립트 실행에 필요한 것들.
# 배포에는 쓰지 않는다(Streamlit Community Cloud는 requirements.txt만 설치).

-r requirements.txt

# --- 1차 벤치마크에 쓴 추가 모델 -----------------------------------------
catboost==1.2.10
pytorch-tabnet==4.1.0
torch==2.13.0

# --- 튜닝 / 불균형 처리 ---------------------------------------------------
optuna==4.9.0
imbalanced-learn==0.14.2

# --- 통계 검증 (OLS 고정효과, 혼합효과모형) -------------------------------
statsmodels==0.14.6

# --- 시각화 / 노트북 ------------------------------------------------------
seaborn==0.13.2
jupyterlab
ipykernel
