# 배포 런타임 의존성 — 대시보드(app_ui.py) + 추론 API(app_api.py) 구동에 필요한 것만.
# 저장된 파이프라인(models/*.pkl)은 학습 당시 버전에 묶여 있으므로 정확히 고정한다.
# 분석 파이프라인 재현에는 requirements-dev.txt를 사용한다.

# --- 웹 계층 -----------------------------------------------------------
streamlit==1.62.0
plotly==6.9.0
fastapi==0.141.1
uvicorn==0.52.4
requests==2.34.2
python-dotenv==1.2.3

# --- 데이터 -------------------------------------------------------------
pandas==3.0.5
numpy==2.4.6
pyarrow==25.0.1
scipy==1.17.1

# --- 모델 (pkl 역직렬화 호환성 때문에 정확한 버전 고정 필수) -------------
scikit-learn==1.9.0
lightgbm==4.7.0
xgboost==3.2.0
joblib==1.5.3

# --- 모델 해석 ----------------------------------------------------------
shap==0.51.0
matplotlib==3.11.1
